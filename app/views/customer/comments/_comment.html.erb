<% product_comments.each do |comment|%>
<ul>
<div class="clearfix">
  <li class="comment-id" style="float: left;">会員ID：<strong><%= comment.customer.id %></strong></li>
  <li class="comment-date" style="float: left;"><%= comment.created_at.strftime('%Y/%m/%d') %></li>
  <% if comment.customer == current_customer %>
  <li class="comment-delete" style="float: left;"><%= link_to "<削除>", customer_product_comment_path(product, comment), method: :delete, remote: true %></li>
  <% end %>
<% end %>
</div>
  <li class="comment-entry"><%= comment.comment %></li>
</ul>