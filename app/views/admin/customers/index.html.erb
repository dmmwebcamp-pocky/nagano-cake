<div class="contanier-fluid">
  <div class="row">
  	<div class="title col-xs-offset-2 col-xs-8">
      <div class="index-header">
        <h3>会員一覧</h3>
      </div>
    </div>
  </div>

  <%= render 'customer_index', customers: @customers %>
<!-- 
  <div class="row">
    <div class="col-xs-offset-2 col-xs-8">
      <table class="table table-hover" style="width: 800px;  height: 100px;">
      	<thead>
	      <tr class="active">
	        <th style="border:none; text-align: left ">会員ID</th>
	      	<th style="border:none; text-align: left">氏名</th>
	      	<th style="border:none; text-align: left">メールアドレス</th>
	      	<th style="border:none; text-align: left">ステータス</th>
	      </tr>
	    </thead>
	    <tbody>
	      <% @customers.each do |customer| %>
	      <tr>
	      	<td><%= customer.id %></td>
	      	<td>
	      		<%= link_to (admin_customer_path(customer.id)) do %>
	      		<%= customer.first_name %> <%= customer.last_name %>
	      		<% end %>
	      	</td>
	      	<td><%= customer.email %></td>
	      	<% if customer.customer_status == "有効" %>
	      	<td>有効</td>
	      	<% else %>
	      	<td>退会済</td>
	      	<% end %>
	      </tr>
	      <% end %>
      	</tbody>
      </table>
    </div>
  </div>
 -->
</div>